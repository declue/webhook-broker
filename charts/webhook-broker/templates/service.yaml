apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-broker.fullname" . }}
  labels:
    {{- include "webhook-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webhook-broker.selectorLabels" . | nindent 4 }}
